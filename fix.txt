- kiến trúc bắt buộc của Blazor WebAssembly Hosted không cho phép
+ chamcong --> chạy trên sever asp.net core, controller, db
+ chamcong.client --> chạy trên trình duyệt webassembly
+ chamcong.share --> dùng chung models và dtos  

Vấn đề cần chú ý / gợi ý cải tiến		
•	Xác thực/ủy quyền: controller không hiển thị auth attributes — cần bảo vệ endpoint (JWT/Cookie, policy).
•	Kích thước/định dạng ảnh và kiểm duyệt input (tránh header injection).
•	Quyền riêng tư: face descriptor/ảnh nhạy cảm — cân nhắc mã hóa hoặc quyền truy cập.
•	Scale: lưu ảnh trên cloud blob storage khi cần mở rộng, thay vì wwwroot.
Mozilla/5.0 (iPhone; CPU iPhone OS 17_5_1 like Mac OS X) AppleWebKit/605.1.15 (KHTML, like Gecko) Version/17.5 Mobile/15E148 Safari/604.1|Apple Computer, Inc.|undefined|Gecko|iPhone	
 
2w1h
- nó là cái gì
- khi nào dùng nó
- dùng nó thì lợi ích như thế nào


hiện tại ứng dụng của tôi đã có chức năng xuất ra excel và từ excel tôi sẽ in ra bảng lương, giờ tôi muốn thêm 
chức năng in trực tiếp tại ứng dụng luôn có được không, phân tích bằng tiếng việt 

ứng dụng lần đầu chạy khi vào chấm công thì còn khá chậm và không load lên được thư viện nhận diện khuôn mặt, sau
khi tắt app mở lại thì nhận diện được khuôn mặt nhưng không tìm được khuôn mặt trùng khớp từ db lên, nhưng tắt app
mở lại lần 2 thì nó mượt hơn, và sau khi tắt ứng dụng chạy trên pc và sau đó mở lại thì nó lại như ban đầu

- tính số giờ làm mặc định là 8 tiếng vậy cột tổng giờ phải từ 8 tiếng trở lên thì mới tính đó là 1 ngày công 
còn nếu tổng giờ dưới 8 tiếng thì tính đó là số giờ tăng ca (có thể cài đặt được sai số là 1 lợi thế)

- lần đầu tiên đăng nhập vào app không quét được khuôn mặt khi chấm công phải refesh lại ứng dụng thì chấm công
mới được và sau khi quét xong thì thông báo không phát hiện khuôn mặt phải chấm công thêm 1 lần nữa thì mới thành
công.
